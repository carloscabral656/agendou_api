# Async driver for the FastAPI app (SQLAlchemy async)
DATABASE_URL=postgresql+asyncpg://admin:admin123@localhost:5432/agendou_db

# JWT (use a long random secret in production)
JWT_SECRET_KEY=dev-change-me-use-at-least-32-chars-long!!
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
PASSWORD_RESET_TOKEN_EXPIRE_MINUTES=60
